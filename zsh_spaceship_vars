# SPACESHIP_PROMPT_ORDER=(user host dir git python exec_time node line_sep jobs exit_code char)
SPACESHIP_PROMPT_ORDER=(user host dir git python venv line_sep jobs exit_code char exec_time)
SPACESHIP_CHAR_SYMBOL_ROOT="#"

SPACESHIP_USER_SHOW="never"
SPACESHIP_USER_COLOR="blue"

# SPACESHIP_HOST_SHOW="true"  # Default, only shows it when SSH'ing
# SPACESHIP_HOST_COLOR="yellow"
SPACESHIP_HOST_COLOR_SSH="yellow"

SPACESHIP_DIR_TRUNC=0
SPACESHIP_DIR_COLOR="blue"

SPACESHIP_JOBS_SYMBOL="(bg jobs)"
SPACESHIP_JOBS_COLOR="red"

SPACESHIP_EXIT_CODE_SHOW="true"
SPACESHIP_EXIT_CODE_PREFIX="["
SPACESHIP_EXIT_CODE_SUFFIX="]"
SPACESHIP_EXIT_CODE_SYMBOL=""
SPACESHIP_EXIT_CODE_COLOR="grey"
SPACESHIP_EXEC_TIME_COLOR="grey"

SPACESHIP_PYTHON_PREFIX=""
SPACESHIP_PYTHON_SYMBOL="🐍 "

SPACESHIP_VENV_GENERIC_NAMES=(virtualenv venv .venv)
SPACESHIP_VENV_PREFIX="venv: "
SPACESHIP_VENV_SYMBOL=""
SPACESHIP_VENV_COLOR="magenta"

SPACESHIP_RUBY_SHOW=true

# Slows down startup:
SPACESHIP_AWS_SHOW="false"
SPACESHIP_DOCKER_SHOW="false"
SPACESHIP_CONDA_SHOW="false"
SPACESHIP_KUBECTL_SHOW="false"

SPACESHIP_GIT_STATUS_UNTRACKED="U"
SPACESHIP_GIT_STATUS_MODIFIED="M"
SPACESHIP_GIT_STATUS_SHOW=false
SPACESHIP_GIT_BRANCH_ASYNC=true
SPACESHIP_GIT_BRANCH_COLOR=cyan

SPACESHIP_PROMPT_ASYNC=true  # Solves the "..." on the prompt, but it slows it down
